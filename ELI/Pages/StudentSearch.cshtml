@page
@model ELI.Pages.StudentSearchModel
@{
    ViewData["Title"] = "StudentSearch";
}

<h2>StudentSearch Test</h2>

<ul>
    @foreach (var student in Model.StudentData)
    {
        <li>@student.LastName, @student.FirstName</li>
    }
</ul>